[project]
name = "universal-master-rpi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gmqtt>=0.7.0",
    "pymodbus>=3.11.3",
    "pyserial>=3.5",
]
